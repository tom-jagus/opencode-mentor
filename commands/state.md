---
description: Mentor - Report current project and repository state
agent: lead
---

Load the `project-progress` skill and execute its State procedure.

This command is read-only. Do not create, modify, stage, commit, push or otherwise mutate files or repository state.

This command accepts no arguments, file references, or attachments.

Do not use Bash to identify files, attachments, or command arguments.

Report inconsistencies between authoritative project artifacts rather than silently choosing one value.
